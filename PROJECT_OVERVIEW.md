# 📱 Laravel E-Commerce Project - شرح شامل للمشروع

**التاريخ:** 14 يناير 2026  
**الإصدار:** Laravel 11.x  
**قاعدة البيانات:** MySQL (jkhsfi)

---

## 📋 نظرة عامة

هذا مشروع **متجر إلكتروني متكامل** مبني على Laravel Framework، متخصص في بيع **الهواتف المحمولة** والمنتجات الإلكترونية. يتضمن نظام إدارة متقدم مع صلاحيات وأدوار، وواجهة عميل سهلة الاستخدام.

### 🎯 الهدف من المشروع
- 🛍️ منصة تجارة إلكترونية لبيع الهواتف والمنتجات
- 📊 لوحة تحكم إدارية قوية
- 🔐 نظام صلاحيات متعدد المستويات
- 📱 دعم خاص للهواتف مع مواصفات تفصيلية
- 🎁 نظام عروض وكوبونات
- ⭐ تقييمات ومراجعات المستخدمين

---

## 🏗️ البنية المعمارية

### 📂 الهيكل الرئيسي

```
laravel_ecommerce_starte/
├── 📁 app/                 # قلب التطبيق (Models, Controllers, Middleware)
├── 📁 resources/           # الواجهات (Views, CSS, JS)
├── 📁 public/              # الملفات العامة (images, assets)
├── 📁 database/            # قاعدة البيانات (migrations, seeders)
├── 📁 routes/              # المسارات (web.php, console.php)
├── 📁 config/              # الإعدادات
├── 📁 storage/             # التخزين (logs, uploads)
├── 📁 scripts/             # سكربتات مساعدة
└── 📁 vendor/              # مكتبات PHP
```

---

## 🗄️ قاعدة البيانات والموديلات

### 📊 الموديلات الرئيسية (18 Model)

#### 👤 **1. User** - المستخدمون
```php
- id, name, email, password
- role_id (علاقة مع Roles)
- العلاقات: orders, reviews, cart, wishlist, addresses
```

#### 🛍️ **2. Product** - المنتجات العامة
```php
- id, name, description, price, stock
- category_id, brand_id
- image, discount_price, is_active
```

#### 📱 **3. Phone** - الهواتف (موديل خاص)
```php
- id, name, brand_id, price, stock
- العلاقات: brand, specs, prices, images
- خصائص: storage, color, screen_size
```

#### 📋 **4. Category** - التصنيفات
```php
- id, name, slug, image
- parent_id (للتصنيفات الفرعية)
```

#### 🏷️ **5. Brand** - العلامات التجارية
```php
- id, name, slug, logo
- مثال: Samsung, Apple, Huawei
```

#### 📦 **6. Order** - الطلبات
```php
- id, user_id, total_amount, status
- shipping_address, payment_method
- العلاقات: items, user
```

#### 🛒 **7. OrderItem** - عناصر الطلب
```php
- id, order_id, product_id, quantity, price
```

#### 🛍️ **8. Cart** - سلة التسوق
```php
- id, user_id, product_id, quantity
- session_id (للزوار)
```

#### ❤️ **9. Wishlist** - المفضلة
```php
- id, user_id, product_id
```

#### ⭐ **10. Review** - التقييمات
```php
- id, user_id, product_id, rating, comment
```

#### 🎁 **11. Offer** - العروض الخاصة
```php
- id, title, discount_percentage
- start_date, end_date, products
```

#### 🎟️ **12. Coupon** - كوبونات الخصم
```php
- id, code, discount, expiry_date
- usage_limit, used_count
```

#### 🔐 **13. Role** - الأدوار
```php
- id, name, description
- مثال: Admin, Manager, Customer
```

#### 🔑 **14. Permission** - الصلاحيات
```php
- id, name, description
- العلاقة: roles (many-to-many)
```

#### 📍 **15. Address** - العناوين
```php
- id, user_id, city, street, postal_code
- is_default
```

#### 📱 **16. PhoneSpec** - مواصفات الهواتف
```php
- id, phone_id, ram, storage, battery
- camera, processor, os
```

#### 💰 **17. PhonePrice** - أسعار الهواتف حسب المواصفات
```php
- id, phone_id, storage_variant, price
```

#### 🏢 **18. PhoneBrand** - علامات الهواتف
```php
- id, name, logo, country
```

#### ⚙️ **19. SiteSetting** - إعدادات الموقع
```php
- id, key, value
- مثال: site_name, contact_email, phone
```

---

## 🔧 التقنيات المستخدمة

### Backend
- ⚡ **Laravel 11.x** - PHP Framework
- 🗄️ **MySQL** - قاعدة البيانات
- 🔐 **Laravel Auth** - نظام المصادقة
- 📧 **Laravel Mail** - إرسال البريد
- 🔄 **Laravel Queue** - قوائم الانتظار

### Frontend
- 🎨 **Blade Templates** - محرك العرض
- ⚡ **Vite** - بناء الأصول
- 💅 **CSS/Bootstrap** - التنسيق
- ⚛️ **JavaScript/Alpine.js** - التفاعلية

### أدوات التطوير
- 🐘 **Composer** - مدير حزم PHP
- 📦 **NPM** - مدير حزم JavaScript
- 🚀 **XAMPP** - بيئة التطوير المحلية
- 📝 **Git** - إدارة النسخ

---

## 📁 شرح المجلدات بالتفصيل

### 1️⃣ **app/** - قلب التطبيق

#### **app/Models/**
جميع موديلات قاعدة البيانات (19 ملف):
- `User.php` - إدارة المستخدمين
- `Product.php` - المنتجات العامة
- `Phone.php` - الهواتف (موديل خاص)
- `Order.php`, `OrderItem.php` - الطلبات
- `Cart.php`, `Wishlist.php` - السلة والمفضلة
- `Category.php`, `Brand.php` - التصنيفات
- `Review.php`, `Offer.php`, `Coupon.php` - التقييمات والعروض
- `Role.php`, `Permission.php` - الصلاحيات
- `Address.php` - العناوين
- `PhoneSpec.php`, `PhonePrice.php`, `PhoneBrand.php` - بيانات الهواتف
- `SiteSetting.php` - إعدادات الموقع

#### **app/Http/Controllers/**
المتحكمات التي تدير منطق الأعمال:
- `ProductController.php` - إدارة المنتجات
- `OrderController.php` - إدارة الطلبات
- `CartController.php` - سلة التسوق
- `UserController.php` - إدارة المستخدمين
- `AdminController.php` - لوحة التحكم
- وغيرها...

#### **app/Http/Middleware/**
الوسائط (Middleware):
- التحقق من الصلاحيات
- الحماية من CSRF
- التحقق من تسجيل الدخول

---

### 2️⃣ **resources/** - الواجهات

```
resources/
├── views/          # ملفات Blade (HTML)
│   ├── layouts/    # القوالب الأساسية
│   ├── admin/      # لوحة التحكم
│   ├── products/   # صفحات المنتجات
│   ├── cart/       # السلة
│   └── auth/       # تسجيل الدخول
├── css/            # ملفات CSS
└── js/             # ملفات JavaScript
```

---

### 3️⃣ **public/** - الملفات العامة

```
public/
├── index.php       # نقطة الدخول الرئيسية
├── images/         # الصور
│   ├── products/   # صور المنتجات
│   ├── phones/     # صور الهواتف
│   └── brands/     # شعارات العلامات
├── css/            # CSS المجمّع
├── js/             # JavaScript المجمّع
└── build/          # ملفات Vite المجمعة
```

---

### 4️⃣ **database/** - قاعدة البيانات

```
database/
├── migrations/     # ملفات إنشاء الجداول
├── seeders/        # بيانات تجريبية
├── factories/      # مصانع البيانات الوهمية
└── dumps/          # نسخ احتياطية
    ├── jkhsfi.sql          # نسخة كاملة
    └── jkhsfi_clean.sql    # نسخة نظيفة
```

#### أهم الـ Migrations:
- `create_users_table.php`
- `create_products_table.php`
- `create_phones_table.php`
- `create_orders_table.php`
- `create_categories_table.php`
- `create_roles_permissions_tables.php`

---

### 5️⃣ **routes/** - المسارات

#### **routes/web.php**
جميع مسارات الموقع:
```php
// الصفحة الرئيسية
Route::get('/', [HomeController::class, 'index']);

// المنتجات
Route::get('/products', [ProductController::class, 'index']);
Route::get('/product/{id}', [ProductController::class, 'show']);

// السلة
Route::post('/cart/add', [CartController::class, 'add']);
Route::get('/cart', [CartController::class, 'index']);

// الطلبات
Route::middleware(['auth'])->group(function () {
    Route::get('/orders', [OrderController::class, 'index']);
    Route::post('/checkout', [OrderController::class, 'store']);
});

// لوحة التحكم (Admin)
Route::middleware(['auth', 'admin'])->prefix('admin')->group(function () {
    Route::resource('products', AdminProductController::class);
    Route::resource('orders', AdminOrderController::class);
    Route::resource('users', UserController::class);
});
```

---

### 6️⃣ **config/** - الإعدادات

الملفات الرئيسية:
- `app.php` - إعدادات التطبيق العامة
- `database.php` - إعدادات قاعدة البيانات
- `auth.php` - إعدادات المصادقة
- `mail.php` - إعدادات البريد الإلكتروني
- `filesystems.php` - إعدادات التخزين
- `session.php` - إعدادات الجلسات

---

### 7️⃣ **scripts/** - سكربتات مساعدة

```
scripts/
├── automate_order.ps1          # أتمتة الطلبات
├── check_permissions.php       # فحص الصلاحيات
├── check_phones.php            # فحص بيانات الهواتف
├── create_test_user.php        # إنشاء مستخدم تجريبي
└── simulate_postman.ps1        # محاكاة API
```

---

## 🔐 نظام الصلاحيات والأدوار

### الأدوار المتاحة:
1. **Super Admin** - صلاحيات كاملة
2. **Admin** - إدارة المحتوى
3. **Manager** - إدارة الطلبات
4. **Customer** - مستخدم عادي

### الصلاحيات:
- `view-products` - عرض المنتجات
- `create-product` - إضافة منتج
- `edit-product` - تعديل منتج
- `delete-product` - حذف منتج
- `manage-orders` - إدارة الطلبات
- `manage-users` - إدارة المستخدمين

---

## 🚀 كيفية التشغيل

### المتطلبات:
- ✅ PHP 8.2+
- ✅ Composer
- ✅ MySQL
- ✅ Node.js & NPM
- ✅ XAMPP (اختياري)

### خطوات التشغيل:

#### 1️⃣ **تثبيت المكتبات**
```bash
composer install
npm install
```

#### 2️⃣ **إعداد البيئة**
```bash
cp .env.example .env
php artisan key:generate
```

#### 3️⃣ **إعداد قاعدة البيانات**
```env
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=jkhsfi
DB_USERNAME=root
DB_PASSWORD=
```

#### 4️⃣ **تشغيل Migrations**
```bash
php artisan migrate --seed
```

#### 5️⃣ **بناء الأصول**
```bash
npm run dev      # للتطوير
npm run build    # للإنتاج
```

#### 6️⃣ **تشغيل السيرفر**
```bash
php artisan serve
```

🌐 **الموقع:** http://localhost:8000

---

## 📝 المميزات الرئيسية

### 🛍️ للعملاء:
- ✅ تصفح المنتجات والهواتف
- ✅ إضافة إلى السلة والمفضلة
- ✅ إتمام الطلبات
- ✅ تتبع الطلبات
- ✅ كتابة التقييمات
- ✅ استخدام الكوبونات

### 👨‍💼 للإداريين:
- ✅ إدارة المنتجات والهواتف
- ✅ إدارة الطلبات (قبول/رفض)
- ✅ إدارة المستخدمين
- ✅ إدارة التصنيفات والعلامات
- ✅ إنشاء العروض والكوبونات
- ✅ تقارير المبيعات
- ✅ إدارة الصلاحيات

---

## 📊 ملفات التوثيق الإضافية

في المشروع:
- 📘 `DEPLOYMENT_GUIDE.md` - دليل النشر
- 📗 `IMPROVEMENTS.md` - التحسينات المطبقة
- 📙 `FIXES_APPLIED.md` - الإصلاحات
- 📕 `ROLES_PERMISSIONS_DASHBOARD_FIXES.md` - إصلاحات الصلاحيات
- 📓 `CATEGORY_IMAGES_UPDATE.md` - تحديث صور التصنيفات

---

## 🛠️ سكربتات التحديث

الملفات الموجودة:
- `update_images.php` - تحديث الصور
- `update_images_unsplash.php` - جلب صور من Unsplash
- `update_category_images.php` - تحديث صور التصنيفات
- `fix_iphone_names.php` - إصلاح أسماء الآيفون
- `add_tablets_category.php` - إضافة تصنيف التابلت
- `optimize.php` - تحسين الأداء

---

## 📦 النشر (Deployment)

### الملفات المتاحة:
- `deploy.sh` - سكربت نشر Linux
- `deploy_to_server.ps1` - سكربت نشر Windows
- `deploy_manual.ps1` - نشر يدوي
- `quick_deploy.sh` - نشر سريع

### خطوات النشر:
```powershell
# Windows
.\deploy_to_server.ps1

# Linux
./deploy.sh
```

---

## 🔗 روابط مهمة

### ملفات الاتصال بالسيرفر:
- `connect_server.bat` - اتصال بالسيرفر
- `update_server_commands.txt` - أوامر التحديث

---

## 📞 الدعم والمساعدة

إذا واجهت أي مشكلة:
1. تحقق من ملفات التوثيق
2. راجع ملفات `FIXES_APPLIED.md`
3. تحقق من ملفات الـ logs في `storage/logs/`

---

## 🎯 الخلاصة

هذا مشروع **متجر إلكتروني متكامل** يحتوي على:
- 📱 19 موديل لقاعدة البيانات
- 🎨 واجهة أمامية وخلفية
- 🔐 نظام صلاحيات متقدم
- 📦 نظام طلبات كامل
- 🛍️ تخصص في الهواتف المحمولة
- 🚀 جاهز للنشر والتطوير

---

**آخر تحديث:** 14 يناير 2026  
**النسخة:** 1.0  
**قاعدة البيانات:** jkhsfi

🎉 **مشروع جاهز للعمل!**
